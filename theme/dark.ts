import { colors } from '@constants';

const darkTheme = {
  name: 'dark',
  dark: true,
  colors: {
    fontPrimary: colors.white,
    fontSecondary: colors.whiteOpacity,
    bgPrimary: colors.dark,
    bgSecondary: colors.lightDark,
  },
};

export default darkTheme;
